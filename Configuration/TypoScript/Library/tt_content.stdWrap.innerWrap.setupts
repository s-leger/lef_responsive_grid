####################################
# Gridelements innerWrap setup
####################################
tt_content.stdWrap.innerWrap{
    cObject{

    # Added by lef_bootstrap3_grid to allow some gridelements to disable innerWrap
    stdWrap.if{
        value = 
        isInList.field = tx_gridelements_backend_layout
        negate = 1
        }
    }   
} 